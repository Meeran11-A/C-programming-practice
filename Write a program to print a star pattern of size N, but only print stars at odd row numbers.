#include <stdio.h>
int main() {
    int N, i, j;
    printf("Enter N: ");
    scanf("%d", &N);
    for(i = 1; i <= N; i++) {
        if(i % 2 != 0) {   // Print only odd rows
            for(j = 1; j <= N; j++) {
                printf("* ");
            }
            printf("\n");
        }
    }
    return 0;
}
